---
import ArrowRightUp from './svgs/ArrowRightUp.astro'

interface Props {
	label: string
	href: string
}
const { label, href } = Astro.props
---

<a class="flex gap-1 font-medium text-slate-200 underline hover:text-teal-300" href={href}>
	{label}
	<ArrowRightUp size="16" />
</a>
